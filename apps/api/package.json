{
  "name": "@yappa/api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "PORT=${API_PORT:-3101} bun --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun dist/index.js",
    "check": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@yappa/db": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "hono": "^4.13.3",
    "openai": "^7.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "6.0.3"
  }
}
