# Documentation

Project documentation lives here.

## Google OAuth

Create a Google OAuth **Web application** client with `http://localhost:3101/auth/google/callback` as an authorized redirect URI. Copy its client ID and a freshly generated client secret into your untracked root `.env` using `.env.example` as the template.

Podcasts are globally readable learning content, including existing local episodes. Interests, costs, creation, deletion, and article generation remain account-scoped.
